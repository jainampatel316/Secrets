# Environment Configuration Example
# Copy this file to .env and update with your actual values

# Application Environment
NODE_ENV=development
PORT=3000

# Security Keys (IMPORTANT: Change these in production!)
# Generate secure random strings for production use:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-change-in-production-please
JWT_SECRET=your-super-secret-jwt-key-change-in-production-please

# Database Configuration (for future use)
# DATABASE_URL=mongodb://localhost:27017/secrets
# DATABASE_URL=postgresql://username:password@localhost:5432/secrets

# Email Configuration (for future email verification)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# Redis Configuration (for session store - future)
# REDIS_URL=redis://localhost:6379

# Rate Limiting (for future implementation)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Logging Configuration
# LOG_LEVEL=info
# LOG_FORMAT=combined

# Security Configuration
# BCRYPT_SALT_ROUNDS=12
# JWT_EXPIRES_IN=24h
# SESSION_MAX_AGE=86400000

# CORS Configuration (if needed for API)
# CORS_ORIGIN=http://localhost:3000
# CORS_CREDENTIALS=true
